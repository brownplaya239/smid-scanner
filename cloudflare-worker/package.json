{
  "name": "smid-scanner-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for ad-hoc ticker dispatch + Yahoo quote/candle proxy",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "tail":   "wrangler tail",
    "login":  "wrangler login",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
